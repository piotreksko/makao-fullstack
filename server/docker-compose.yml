services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: makao
      POSTGRES_PASSWORD: makao
      POSTGRES_DB: makao
    ports: ["5432:5432"]
    volumes: ["pgdata:/var/lib/postgresql/data"]
  redis:
    image: redis:7
    ports: ["6379:6379"]
volumes:
  pgdata:
